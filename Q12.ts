// Question 12 : Names & Message

const names : string[] = ["Owais" , "Mubeen" , "Saim"];
const message : string = "Hello";

for(const name of names) {
    console.log(message + " "  + name);
    
}